/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['links.papareact.com','i.postimg.cc','upload.wikimedia.org','cloud.appwrite.io'],
    }
}

module.exports = nextConfig
